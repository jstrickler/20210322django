"""
URL Configuration for tables
"""
from django.urls import path
from . import views   # import views from app

app_name = "tables"

urlpatterns = [
    # add url patterns for the tables app here

    # Examples:
    # path('', views.home, name='home'),
    # path('thing', views.thing, name='thing'),
]
